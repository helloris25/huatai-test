# BrightnessMeter

```svelte
<BrightnessMeter
                {min}
                {max}
                {value}
                {optimum}
                on:changeValue={event => logEvent('changeValue', event)}/>
```
